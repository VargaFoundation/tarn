Tarn a ete deploye avec succes !

1. Verifier le statut des pods :
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "tarn.name" . }}

2. Acceder au service :
   kubectl port-forward svc/{{ include "tarn.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Assurez-vous que la configuration Hadoop (core-site.xml, yarn-site.xml) est accessible au pod
via un volume mount ou un ConfigMap.
{{- $ss := .Values.config.sharedState | default "local" }}
{{- if and (gt (int .Values.replicaCount) 1) (ne $ss "zk") }}

⚠️  replicaCount={{ .Values.replicaCount }} mais config.sharedState="{{ $ss }}".
    En mode "local", chaque replique applique quotas et rate-limit independamment
    (limites ~{{ .Values.replicaCount }}x trop permissives) et l'affinite de conversation n'est pas partagee.
    Passez config.sharedState=zk (reutilise zookeeperQuorum) pour une application coherente sur tout le parc.
{{- end }}
