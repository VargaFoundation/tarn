apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tarn.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tarn.labels" . | nindent 4 }}
data:
  HADOOP_CONF_DIR: {{ .Values.config.hadoopConfDir | quote }}
  YARN_RESOURCEMANAGER_ADDRESS: {{ .Values.config.yarnResourceManagerAddress | quote }}
  ZK_ENSEMBLE: {{ .Values.config.zookeeperQuorum | quote }}
  TRITON_IMAGE: {{ .Values.config.tritonDockerImage | quote }}
  MODEL_REPOSITORY: {{ .Values.config.modelStoragePath | quote }}
  # TLS
  TLS_ENABLED: {{ .Values.config.tlsEnabled | quote }}
  {{- if .Values.config.tlsKeystorePath }}
  TLS_KEYSTORE_PATH: {{ .Values.config.tlsKeystorePath | quote }}
  {{- end }}
  # South-side TLS (AM/proxy → Triton)
  TRITON_TLS_ENABLED: {{ .Values.config.tritonTlsEnabled | default false | quote }}
  {{- if .Values.config.tritonTlsTruststorePath }}
  TRITON_TLS_TRUSTSTORE_PATH: {{ .Values.config.tritonTlsTruststorePath | quote }}
  {{- end }}
  {{- if .Values.config.tritonTlsClientKeystorePath }}
  TRITON_TLS_CLIENT_KEYSTORE_PATH: {{ .Values.config.tritonTlsClientKeystorePath | quote }}
  {{- end }}
  # OpenAI proxy
  OPENAI_PROXY_ENABLED: {{ .Values.config.openaiProxyEnabled | quote }}
  EMBEDDING_CACHE_SIZE: {{ .Values.config.embeddingCacheSize | default 0 | quote }}
  # OTEL
  {{- if .Values.config.otelEndpoint }}
  OTEL_EXPORTER_OTLP_ENDPOINT: {{ .Values.config.otelEndpoint | quote }}
  {{- end }}
  # Scaling
  SCALE_MODE: {{ .Values.config.scaleMode | quote }}
  # Cross-replica enforcement: local (default) | zk. "zk" reuses ZK_ENSEMBLE above so the global
  # rate limit and per-user quotas are fair-shared across replicas and conversation affinity is
  # shared. Recommended whenever replicaCount > 1.
  SHARED_STATE: {{ .Values.config.sharedState | default "local" | quote }}
  # Safety defaults
  RANGER_STRICT: {{ .Values.config.rangerStrict | quote }}
  ZK_REQUIRED: {{ .Values.config.zkRequired | quote }}
  {{- if .Values.extraJavaOpts }}
  JAVA_TOOL_OPTIONS: {{ .Values.extraJavaOpts | quote }}
  {{- end }}
